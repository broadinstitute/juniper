{
  "name": "@juniper/e2e-tests",
  "version": "0.0.1",
  "scripts": {
    "build": "npx tsc --build",
    "postinstall": "npx playwright install --with-deps",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
