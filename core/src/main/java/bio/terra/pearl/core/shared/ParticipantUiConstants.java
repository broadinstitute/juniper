package bio.terra.pearl.core.shared;

/** Constants that need to be known across the entire app, so that, e.g., the app can generate emails with links */
public class ParticipantUiConstants {
    public static final String DASHBOARD_PATH = "/hub";
    public static final String LOCAL_DEV_HOSTNAME = "localhost:3001";
}
